fastapi>=0.110
uvicorn[standard]>=0.27
pandas>=2.0
openpyxl>=3.1
rapidfuzz>=3.5
scikit-learn>=1.3
rank-bm25>=0.2.2
unidecode>=1.3
numpy>=1.24
# Semantic embeddings (optional — TF-IDF fallback if unavailable):
sentence-transformers>=2.6
# LLM boost (optional — deterministic core runs without it):
openai>=1.20
