info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/health"
  body:
    type: json
    data: |-
      {
        "data": {
          "test": "test"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
