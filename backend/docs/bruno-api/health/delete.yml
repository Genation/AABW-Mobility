info:
  name: delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{host}}/health/:id"
  params:
    - name: id
      value: "3"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
